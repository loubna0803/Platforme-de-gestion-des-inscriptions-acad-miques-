server:
  port: 8081

spring:
  application:
    name: student-service
  datasource:
    url: jdbc:mysql://localhost:3306/studentdb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
